% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny-timeline.R
\name{updateGgWebGLTimeline}
\alias{updateGgWebGLTimeline}
\title{Update a ggWebGL Timeline from Shiny}
\usage{
updateGgWebGLTimeline(
  session,
  outputId,
  value = NULL,
  index = NULL,
  playing = NULL,
  speed = NULL,
  loop = NULL
)
}
\arguments{
\item{session}{A Shiny session object.}

\item{outputId}{Output id passed to \code{\link[=ggWebGLOutput]{ggWebGLOutput()}}. Inside Shiny modules,
pass the un-namespaced id; \code{session$ns()} is applied by this helper.}

\item{value}{Optional frame or time value to select.}

\item{index}{Optional 1-based timeline index to select. Exactly one of
\code{value} or \code{index} may be supplied.}

\item{playing}{Optional logical scalar controlling playback.}

\item{speed}{Optional positive playback-speed multiplier.}

\item{loop}{Optional logical scalar controlling loop playback.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Send a timeline update message to a rendered ggWebGL widget. The browser
applies the update to the widget identified by \code{outputId} and emits the
updated state back as \verb{input$<outputId>_timeline}.
}
\examples{
\dontshow{if (interactive() && requireNamespace("shiny", quietly = TRUE)) withAutoprint(\{ # examplesIf}
server <- function(input, output, session) {
  shiny::observeEvent(input$next_frame, {
    updateGgWebGLTimeline(session, "plot", index = 2)
  })
}
\dontshow{\}) # examplesIf}
}
