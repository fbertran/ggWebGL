% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magnify-region.R
\name{ggwebgl_magnify_region}
\alias{ggwebgl_magnify_region}
\title{Build a Linked Magnifying-Glass Zoom Scene}
\usage{
ggwebgl_magnify_region(
  source,
  region,
  display = c("panel", "inset"),
  source_panel = NULL,
  zoom_layers = NULL,
  global_panel_id = "global",
  zoom_panel_id = "local",
  global_label = "Global",
  zoom_label = "Zoomed region",
  box = TRUE,
  box_colour = "#334155",
  box_alpha = 0.65,
  box_width = 1.5,
  inset = list(left = 0.68, top = 0.06, width = 0.24, height = 0.24),
  interactive = FALSE,
  width = NULL,
  height = NULL,
  background = "white",
  preset = c("clean", "publication"),
  labels = NULL,
  webgl = NULL
)
}
\arguments{
\item{source}{A \code{ggplot}, \code{ggWebGL} widget, \code{ggwebgl_spec}, or raw renderer
payload accepted by \code{\link[=ggWebGL]{ggWebGL()}}.}

\item{region}{Rectangle to magnify. Use either \code{list(x = c(xmin, xmax), y = c(ymin, ymax))} or \code{list(xmin = ..., xmax = ..., ymin = ..., ymax = ...)}.}

\item{display}{One of \code{"panel"} or \code{"inset"}.}

\item{source_panel}{Optional panel id to magnify when \code{source} has multiple
panels. Defaults to the first panel.}

\item{zoom_layers}{Optional renderer-ready layers to use in the zoom view.
When omitted, the source panel layers are reused.}

\item{global_panel_id, zoom_panel_id}{Panel ids used in \code{display = "panel"}.}

\item{global_label, zoom_label}{Optional panel labels.}

\item{box}{Whether to add a rectangle overlay to the global panel.}

\item{box_colour, box_alpha, box_width}{Rectangle styling.}

\item{inset}{Inset placement list for \code{display = "inset"} with fractional
\code{left}, \code{top}, \code{width}, and \code{height}.}

\item{interactive}{Whether a two-panel magnifier should let browser-side
brush rectangles on the global panel update the zoom panel viewport live.}

\item{width, height}{Optional publication figure dimensions for inset output.}

\item{background, preset}{Publication figure styling for inset output.}

\item{labels}{Optional labels for the derived renderer specs.}

\item{webgl}{Optional renderer options for the derived specs. Defaults to
the source webgl options.}
}
\value{
A \code{ggwebgl_spec} for \code{display = "panel"} or a
\code{ggwebgl_publication_figure} for \code{display = "inset"}.
}
\description{
Create a deterministic zoom view from a rectangular data region. The helper
is renderer-generic: callers provide renderer-ready ggWebGL sources and the
selected region, and ggWebGL derives either a two-panel zoom spec or a
publication figure with a linked inset.
}
\examples{
source <- ggwebgl_spec(
  layers = list(
    ggwebgl_layer_points(
      data.frame(x = c(0, 1, 2, 3), y = c(0, 2, 1, 3)),
      x = "x",
      y = "y",
      colour = "#2563eb",
      alpha = 0.75,
      size = 4
    )
  )
)

ggwebgl_magnify_region(
  source,
  region = list(x = c(0.75, 2.25), y = c(0.75, 2.25)),
  display = "panel"
)
}
