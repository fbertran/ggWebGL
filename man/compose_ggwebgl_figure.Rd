% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-static.R
\name{compose_ggwebgl_figure}
\alias{compose_ggwebgl_figure}
\title{Compose a Publication Figure from ggWebGL Panels}
\usage{
compose_ggwebgl_figure(
  panels,
  file,
  width = 1800L,
  height = 1200L,
  format = NULL,
  dpi = 300,
  background = "white",
  layout = c("single", "row", "grid"),
  labels = NULL,
  inset = NULL,
  annotations = NULL,
  preset = c("clean", "publication"),
  selfcontained = FALSE,
  wait_seconds = 3,
  nrow = NULL,
  ncol = NULL,
  elementId = NULL
)
}
\arguments{
\item{panels}{A list of panel sources. Each element may be a \code{ggplot},
\code{ggWebGL} widget, \code{ggwebgl_spec}, raw renderer payload, image path, or a
list with \code{source} plus optional \code{wait_seconds}, \code{show_panel_overlay}, and
\code{preset} overrides.}

\item{file}{Output file path.}

\item{width, height}{Output size in pixels.}

\item{format}{Optional image format. When omitted, it is inferred from
\code{file}.}

\item{dpi}{Output density metadata used when writing the image.}

\item{background}{Background colour used for the final flattened image.}

\item{layout}{One of \code{"single"}, \code{"row"}, or \code{"grid"}.}

\item{labels}{Optional character vector of panel labels drawn in the top-left
corner of each occupied panel cell.}

\item{inset}{Optional list with a panel \code{source}, fractional \code{left}, \code{top},
\code{width}, \code{height}, and optional \code{border}, \code{border_colour}, and
\code{border_alpha}.}

\item{annotations}{Optional list of text annotations. Each entry should
contain \code{text} plus fractional \code{x} and \code{y}, with optional \code{size},
\code{colour}, \code{font}, \code{hjust}, and \code{vjust}.}

\item{preset}{Export preset. \code{"publication"} adds subtle panel borders and
muted label styling.}

\item{selfcontained}{Passed through to \code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}} for
temporary widget captures.}

\item{wait_seconds}{Default render delay before capture.}

\item{nrow, ncol}{Optional grid dimensions used when \code{layout = "grid"}.}

\item{elementId}{Optional DOM element id passed when panel sources must first
be turned into widgets.}
}
\value{
The normalized output file path, invisibly.
}
\description{
Capture one or more ggWebGL scenes and assemble them into a single clean
publication image.
}
\examples{
\dontshow{if (requireNamespace("chromote", quietly = TRUE) && requireNamespace("magick", quietly = TRUE) && !nzchar(Sys.getenv("_R_CHECK_PACKAGE_NAME_"))) withAutoprint(\{ # examplesIf}
old <- options(ggwebgl.reset_processx_supervisor = TRUE)
on.exit(options(old), add = TRUE)

point_spec <- ggwebgl_spec(
  layers = list(
    ggwebgl_layer_points(
      data.frame(x = c(0.15, 0.48, 0.82), y = c(0.22, 0.76, 0.38)),
      x = "x",
      y = "y",
      colour = c("#0f766e", "#f97316", "#2563eb"),
      alpha = 0.8,
      size = 5
    )
  ),
  webgl = list(shader = "default", interactions = character())
)
line_spec <- ggwebgl_spec(
  layers = list(
    ggwebgl_layer_lines(
      data.frame(x = c(0.1, 0.45, 0.8), y = c(0.25, 0.75, 0.35)),
      x = "x",
      y = "y",
      colour = "#334155",
      alpha = 0.9,
      width = 2
    )
  ),
  webgl = list(shader = "default", interactions = character())
)

out <- tempfile(fileext = ".jpg")
compose_ggwebgl_figure(
  panels = list(
    point_spec,
    line_spec
  ),
  file = out,
  layout = "row",
  labels = c("points", "lines"),
  width = 480,
  height = 240,
  format = "jpeg",
  preset = "clean",
  wait_seconds = 0.25
)
file.exists(out)
\dontshow{\}) # examplesIf}
}
