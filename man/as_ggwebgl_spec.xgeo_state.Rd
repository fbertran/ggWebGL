% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-xgeo-state.R
\name{as_ggwebgl_spec.xgeo_state}
\alias{as_ggwebgl_spec.xgeo_state}
\title{Convert an \code{xgeo_state} object to a ggWebGL renderer specification}
\usage{
\method{as_ggwebgl_spec}{xgeo_state}(
  x,
  embedding = NULL,
  primitive = c("points", "density", "surface"),
  lod = NULL,
  webgl = list(),
  labels = list(),
  point_size = 4,
  alpha = 0.85,
  ...
)
}
\arguments{
\item{x}{An \code{xgeo_state} object.}

\item{embedding}{Optional embedding name. Defaults to the active embedding.}

\item{primitive}{Primitive family to project to renderer payloads.}

\item{lod}{Optional LOD selector for \code{primitive = "density"}. Accepts
\code{NULL}, a single bundle name, a \code{bundle/level} string, or a list with
\code{name} and optional \code{level}.}

\item{webgl}{Renderer options passed through \code{normalise_webgl_options()}.}

\item{labels}{Optional labels list (\code{title}, \code{subtitle}, \code{x}, \code{y}) that
overrides metadata-derived defaults.}

\item{point_size}{Point size used for point payloads.}

\item{alpha}{Alpha used for generated payload colors.}

\item{...}{Reserved for future adapters.}
}
\value{
A normalized ggWebGL renderer specification.
}
\description{
Convert an \code{xgeo_state} object to a ggWebGL renderer specification
}
\examples{
toy_state <- list(
  attributes = list(
    embeddings = list(
      active = "toy",
      items = list(
        toy = list(
          coords = data.frame(
            point_id = paste0("p", 1:4),
            dim1 = c(0, 1, 0, 1),
            dim2 = c(0, 0, 1, 1)
          )
        )
      )
    ),
    explanations = data.frame(
      point_id = paste0("p", 1:4),
      value = c(0.2, 0.4, 0.8, 0.5)
    )
  ),
  metadata = list(title = "Toy backend state")
)
class(toy_state) <- "xgeo_state"

xgeo_spec <- as_ggwebgl_spec(toy_state, primitive = "points")
xgeo_spec$render$primitives
}
