% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publication-figure.R
\name{ggwebgl_publication_figure}
\alias{ggwebgl_publication_figure}
\title{Build a Publication-Mode Figure Container from ggWebGL Panels}
\usage{
ggwebgl_publication_figure(
  panels,
  layout = c("single", "row", "grid"),
  labels = NULL,
  annotations = NULL,
  inset = NULL,
  background = "white",
  preset = c("clean", "publication"),
  width = NULL,
  height = NULL
)
}
\arguments{
\item{panels}{A non-empty list of panel sources. Supported sources are
\code{ggplot} objects, \code{ggWebGL} htmlwidgets, \code{ggwebgl_spec} objects, or raw
renderer payloads accepted by \code{\link[=ggWebGL]{ggWebGL()}}. Each element may also be a list
with \code{source} plus optional \code{show_panel_overlay}.}

\item{layout}{One of \code{"single"}, \code{"row"}, or \code{"grid"}.}

\item{labels}{Optional character vector of panel labels.}

\item{annotations}{Optional list of figure-level text annotations. Each entry
should contain \code{text}, \code{x}, and \code{y}, with optional \code{size}, \code{colour},
\code{font}, \code{hjust}, and \code{vjust}.}

\item{inset}{Optional inset specification containing a panel \code{source} plus
fractional \code{left}, \code{top}, \code{width}, and \code{height}.}

\item{background}{Figure background colour.}

\item{preset}{Publication styling preset. \code{"publication"} adds subtle panel
borders and muted overlay text.}

\item{width, height}{Optional figure dimensions in pixels.}
}
\value{
A browsable HTML container with class \code{ggwebgl_publication_figure}.
}
\description{
Create a package-owned HTML container for publication capture. Each child
panel is rendered through ggWebGL in publication mode unless it already
declares a different rendering contract explicitly.
}
\examples{
demo_spec <- ggwebgl_spec(
  layers = list(
    ggwebgl_layer_points(
      data.frame(x = c(0.15, 0.52, 0.84), y = c(0.20, 0.78, 0.42)),
      x = "x",
      y = "y",
      colour = c("#0f766e", "#f97316", "#2563eb"),
      alpha = 0.8,
      size = 5
    )
  )
)

figure <- ggwebgl_publication_figure(
  panels = list(demo_spec),
  width = 420,
  height = 280
)

inherits(figure, "ggwebgl_publication_figure")
}
