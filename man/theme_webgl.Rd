% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-webgl.R
\name{theme_webgl}
\alias{theme_webgl}
\title{Add WebGL Rendering Options to a ggplot}
\usage{
theme_webgl(
  shader = "default",
  antialias = TRUE,
  transparent = TRUE,
  buffer_size = 65536L,
  interactions = c("pan", "zoom"),
  rendering = "visualization",
  panel_overlay = "auto",
  view = NULL,
  selection = NULL,
  dimension = "2d",
  camera = "orbit",
  projection = "orthographic",
  camera_state = list(),
  depth_test = NULL,
  blend_mode = "auto",
  timeline = NULL,
  ...
)
}
\arguments{
\item{shader}{Shader preset name or path identifier. Built-in modes are
\code{"default"}, \code{"density_splat"}, \code{"trajectory_age"}, and
\code{"trajectory_age_glow"}, \code{"trajectory_velocity"}, and
\code{"trajectory_direction"}.}

\item{antialias}{Logical scalar; whether antialiasing should be requested.}

\item{transparent}{Logical scalar; whether the drawing surface should allow
transparency.}

\item{buffer_size}{Integer scalar giving the initial buffer allocation used
by the eventual renderer.}

\item{interactions}{Legacy character vector of interaction modes to enable.
New code should use \code{selection = ggwebgl_selection(...)} for brush/lasso
behavior.}

\item{rendering}{Rendering contract mode. \code{"visualization"} keeps the
current interactive widget chrome. \code{"publication"} suppresses presentation
chrome by default and is intended for clean figure capture.}

\item{panel_overlay}{Panel overlay display mode. \code{"auto"} shows panel strips
and frames for faceted plots, \code{"show"} forces them on, and \code{"hide"}
removes them.}

\item{view}{Optional \code{\link[=ggwebgl_view]{ggwebgl_view()}} object. This is the preferred structured
view/camera contract.}

\item{selection}{Optional \code{\link[=ggwebgl_selection]{ggwebgl_selection()}} object. This is the preferred
selection contract.}

\item{dimension, camera, projection, camera_state}{Legacy view fields retained as
an internal migration shim.}

\item{depth_test}{Logical scalar. \code{NULL} uses the renderer default: disabled
for 2D scenes and enabled for 3D scenes. Set explicitly to override.}

\item{blend_mode}{Primitive blending mode: \code{"auto"}, \code{"alpha"},
\code{"additive"}, or \code{"premultiplied"}.}

\item{timeline}{Optional \code{ggwebgl_timeline()} specification for runtime
playback controls.}

\item{...}{Reserved for future backend-specific options.}
}
\value{
An object that can be added to a \code{ggplot}.
}
\description{
Attach WebGL-specific rendering metadata to a \code{ggplot} object. The returned
object is consumed by \code{\link[=ggplot_webgl]{ggplot_webgl()}} and stored on the plot as
\code{plot$ggwebgl}.
}
\examples{
plot <- ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt, colour = factor(cyl))) +
  ggplot2::geom_point() +
  theme_webgl(
    shader = "density_splat",
    selection = ggwebgl_selection("none")
  )

plot$ggwebgl
}
