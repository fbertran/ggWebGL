% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-static.R
\name{snapshot_ggwebgl}
\alias{snapshot_ggwebgl}
\title{Capture a ggWebGL Scene as a Static Image}
\usage{
snapshot_ggwebgl(
  x,
  file,
  width = 1800L,
  height = 1200L,
  format = NULL,
  dpi = 300,
  background = "white",
  preset = c("clean", "publication"),
  selfcontained = FALSE,
  wait_seconds = 3,
  show_panel_overlay = FALSE,
  elementId = NULL
)
}
\arguments{
\item{x}{A \code{ggplot}, \code{ggWebGL} htmlwidget, \code{ggwebgl_spec}, raw renderer
payload accepted by \code{\link[=ggWebGL]{ggWebGL()}}, or a \code{\link[=ggwebgl_publication_figure]{ggwebgl_publication_figure()}}.}

\item{file}{Output file path.}

\item{width, height}{Output size in pixels.}

\item{format}{Optional image format. When omitted, it is inferred from
\code{file}.}

\item{dpi}{Output density metadata used when writing the image.}

\item{background}{Background colour used for the final flattened image.}

\item{preset}{Export preset. \code{"clean"} removes UI chrome; \code{"publication"}
also applies subtle panel-strip and panel-frame styling for publication
capture.}

\item{selfcontained}{Passed through to \code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}} for the
temporary export widget.}

\item{wait_seconds}{Delay before capture to allow the widget to finish
rendering.}

\item{show_panel_overlay}{Whether facet/panel overlays should remain visible
in the captured output.}

\item{elementId}{Optional DOM element id passed when \code{x} must first be turned
into a widget.}
}
\value{
The normalized output file path, invisibly.
}
\description{
Build a \code{ggWebGL} widget if needed, hide interactive chrome for export, and
capture a clean static image through the browser-backed widget path.
}
\examples{
\dontshow{if (requireNamespace("chromote", quietly = TRUE) && requireNamespace("magick", quietly = TRUE) && !nzchar(Sys.getenv("_R_CHECK_PACKAGE_NAME_"))) withAutoprint(\{ # examplesIf}
old <- options(ggwebgl.reset_processx_supervisor = TRUE)
on.exit(options(old), add = TRUE)

tiny_spec <- ggwebgl_spec(
  layers = list(
    ggwebgl_layer_points(
      data.frame(x = c(0.15, 0.5, 0.82), y = c(0.25, 0.78, 0.4)),
      x = "x",
      y = "y",
      colour = c("#0f766e", "#f97316", "#2563eb"),
      alpha = 0.8,
      size = 5
    )
  ),
  webgl = list(shader = "default", interactions = character())
)

out <- tempfile(fileext = ".jpg")
snapshot_ggwebgl(
  tiny_spec,
  out,
  width = 320,
  height = 220,
  format = "jpeg",
  preset = "clean",
  wait_seconds = 0.25
)
file.exists(out)
\dontshow{\}) # examplesIf}
}
