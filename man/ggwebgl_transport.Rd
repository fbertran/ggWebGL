% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/payload-compact.R
\name{ggwebgl_transport}
\alias{ggwebgl_transport}
\title{Configure ggWebGL Transport Options}
\usage{
ggwebgl_transport(
  mode = c("auto", "compact", "legacy"),
  threshold = 100000L,
  progressive = c("auto", "on", "off"),
  chunk_size = 100000L,
  position = c("float32", "quantized"),
  colors = c("auto", "palette", "rgba"),
  lod = c("auto", "grid", "none"),
  lod_max_points = 5000L
)
}
\arguments{
\item{mode}{Transport mode. \code{"auto"} compacts point layers at or above
\code{threshold}, \code{"compact"} compacts all point layers, and \code{"legacy"} keeps the
original JSON-array payloads.}

\item{threshold}{Row threshold used when \code{mode = "auto"}.}

\item{progressive}{Progressive upload policy. \code{"auto"} enables progressive
upload for compact layers, \code{"on"} forces it, and \code{"off"} disables it.}

\item{chunk_size}{Number of points per progressive upload chunk.}

\item{position}{Position encoding, either \code{"float32"} or \code{"quantized"}.}

\item{colors}{Color encoding. \code{"auto"} uses palette lookup when beneficial and
byte RGBA otherwise; \code{"palette"} requires palette lookup when possible; and
\code{"rgba"} stores byte RGBA.}

\item{lod}{LOD preview policy. \code{"auto"} generates deterministic grid previews
for compact point layers, \code{"grid"} forces them, and \code{"none"} disables them.}

\item{lod_max_points}{Maximum number of preview points generated by the grid
LOD helper.}
}
\value{
A normalized \code{ggwebgl_transport} list.
}
\description{
\code{ggwebgl_transport()} controls how large renderer payloads are encoded before
they are handed to the browser. Small scenes keep the existing JSON arrays by
default; large point layers can use base64 typed arrays, palette color lookup,
deterministic preview LOD, and progressive GPU upload.
}
\examples{
ggwebgl_transport(threshold = 100000L, chunk_size = 50000L)
}
