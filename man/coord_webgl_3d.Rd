% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coord-webgl-3d.R
\name{coord_webgl_3d}
\alias{coord_webgl_3d}
\title{Request a 3D WebGL Coordinate System}
\usage{
coord_webgl_3d(
  projection = c("perspective", "orthographic"),
  camera = c("orbit", "trackball"),
  depth_test = TRUE,
  state = list()
)
}
\arguments{
\item{projection}{Projection mode, \code{"perspective"} or \code{"orthographic"}.}

\item{camera}{3D camera controller, \code{"orbit"} or \code{"trackball"}.}

\item{depth_test}{Logical scalar; whether the browser renderer should enable
depth testing for this scene.}

\item{state}{Optional camera state list passed to \code{\link[=ggwebgl_view]{ggwebgl_view()}}.}
}
\value{
A ggplot-addable \code{ggwebgl_coord_3d} object.
}
\description{
\code{coord_webgl_3d()} is a ggplot-addable helper that marks the WebGL payload as
\code{cartesian3d} and installs a structured 3D view contract. It does not replace
ggplot2's 2D coordinate system; the standard ggplot object remains valid, and
the 3D interpretation is applied by \code{\link[=ggplot_webgl]{ggplot_webgl()}}. Panel ranges and
fixed-scale facet metadata still come from ggplot2's built plot object.
}
\examples{
ggplot2::ggplot(
  data.frame(x = 1:3, y = 1:3, z = c(0, 1, 0)),
  ggplot2::aes(x, y, z = z)
) +
  geom_point_webgl() +
  coord_webgl_3d()
}
