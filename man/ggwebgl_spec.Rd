% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-primitives.R
\name{ggwebgl_spec}
\alias{ggwebgl_spec}
\title{Build a ggWebGL Specification from Renderer-Ready Layers}
\usage{
ggwebgl_spec(
  layers,
  labels = list(),
  webgl = list(),
  grid = NULL,
  panels = NULL,
  messages = character(),
  timeline = NULL
)
}
\arguments{
\item{layers}{A list of normalized point, line, raster, vector, mesh, or surface
layers.}

\item{labels}{Optional labels list (\code{title}, \code{subtitle}, \code{x}, \code{y}).}

\item{webgl}{Optional renderer options passed to \code{\link[=theme_webgl]{theme_webgl()}}.}

\item{grid}{Optional list with \code{rows} and \code{cols}.}

\item{panels}{Optional panel metadata list or data frame with \code{panel_id},
\code{row}, \code{col}, optional \code{label}, and optional \code{viewport}.}

\item{messages}{Optional character vector of renderer messages.}

\item{timeline}{Optional \code{ggwebgl_timeline()} specification.}
}
\value{
A classed \code{ggwebgl_spec} object accepted by \code{\link[=ggWebGL]{ggWebGL()}}.
}
\description{
Build a ggWebGL Specification from Renderer-Ready Layers
}
\examples{
panel_points <- ggwebgl_layer_points(
  data.frame(x = c(0, 1), y = c(1, 0)),
  x = "x",
  y = "y"
)
panel_lines <- ggwebgl_layer_lines(
  data.frame(x = c(0, 1, 2), y = c(0, 1, 0)),
  x = "x",
  y = "y",
  panel_id = "B"
)

spec <- ggwebgl_spec(
  layers = list(panel_points, panel_lines),
  labels = list(title = "adapter spec"),
  panels = data.frame(
    panel_id = c(1L, "B"),
    row = c(1L, 1L),
    col = c(1L, 2L),
    stringsAsFactors = FALSE
  )
)

spec$render$grid
}
