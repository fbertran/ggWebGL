% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeline-webgl.R
\name{scale_time_webgl}
\alias{scale_time_webgl}
\title{Add Timeline Metadata to a ggplot}
\usage{
scale_time_webgl(
  source = c("auto", "frame", "time"),
  values = NULL,
  mode = c("exact", "cumulative"),
  fps = NULL,
  speed = 1,
  loop = FALSE,
  label = NULL,
  format = NULL
)
}
\arguments{
\item{source}{Timeline column source. \code{"auto"} prefers a built \code{time} column
when present and otherwise uses \code{frame}.}

\item{values}{Optional explicit timeline values. When omitted, values are
derived from built layer \code{time} or \code{frame} columns.}

\item{mode}{Timeline filtering intent: \code{"exact"} or \code{"cumulative"}.}

\item{fps}{Optional frames-per-second metadata.}

\item{speed}{Positive playback-speed multiplier.}

\item{loop}{Whether future playback controls should loop.}

\item{label}{Optional timeline label metadata.}

\item{format}{Optional display-format metadata.}
}
\value{
An object that can be added to a \code{ggplot}.
}
\description{
\code{scale_time_webgl()} is not a visual ggplot2 scale. It records timeline
metadata for \code{\link[=ggplot_webgl]{ggplot_webgl()}} so frame or time values can be normalized into
\code{render$timeline}.
}
\examples{
df <- data.frame(x = c(0, 1, 2), y = c(0, 1, 0), frame = 1:3)
plot <- ggplot2::ggplot(df, ggplot2::aes(x, y, frame = frame)) +
  geom_point_webgl() +
  scale_time_webgl(source = "frame", mode = "exact")
plot$ggwebgl$time_scale$source
}
